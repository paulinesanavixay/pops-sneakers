<!-- navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark w-100">
    <div class="container-fluid d-flex align-items-center justify-content-between">

        <!-- Lien vers les paires vendues par la communauté (reste à gauche) -->
        <div class="d-flex align-items-center">
            <a href="{{ path('public_sneakers_list') }}" class="text-white">Sell sneakers from our community</a>
        </div>

        <!-- Logo et liens au centre -->
        <div class="d-flex align-items-center justify-content-center mx-auto">
            <!-- Logo -->
            <a href="{{ path('homepage') }}">
                <img src="{{ asset('images/logo.png') }}" alt="Pop's Sneakers Logo" height="70" class="mx-3">
            </a>
            <!-- Liens de navigation -->
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('homepage') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('brandList') }}">Sneakers</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('sell') }}">Sell</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('aboutpage') }}">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('newsletter') }}">Newsletter</a>
                </li>
            </ul>
        </div>

        <!-- Connexion / Déconnexion à droite -->
        <div class="d-flex align-items-center ms-auto">
            {% if app.user %}
                <span class="text-white">Hi {{ app.user.username }}! </span>
                <a href="{{ path('app_logout') }}" class="text-white ms-2"> Logout</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="text-white">Login </a>
                <a href="{{ path('app_register') }}" class="text-white ms-2"> Register</a>
            {% endif %}
        </div>
    </div>
</nav>

<!-- CSS intégré -->
<style>
    .navbar-nav {
        margin-left: 20px; /* Espacement des liens à gauche du logo */
    }

    .ms-auto {
        margin-left: auto; /* Aligner les éléments de connexion/déconnexion à droite */
    }

    .d-flex.align-items-center.justify-content-center {
        flex-grow: 1; /* Assurer que l'espace entre les sections est bien réparti */
    }
</style>

