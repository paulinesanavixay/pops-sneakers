<form method="post" action="{{ path('app_sneaker_crud_delete', {'id': sneaker.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sneaker.id) }}">
    <button class="btn">Delete</button>
</form>
